/*
package com.edy.core_network_movie.datasource

import com.edy.core_data_model.MovieDTO
import com.edy.core_data_movie.datasource.MovieRemoteDataSource
import com.edy.core_domain.usecase.flow.Resource
import com.edy.network.MovieServices
import javax.inject.Inject


class MovieRemoteDataSourceImp @Inject constructor(
    private val movieService: MovieServices,
): MovieRemoteDataSource {

    override suspend fun getMovies(): Resource<List<MovieDTO>> {
        return movieService.getFilms()
    }
}
*/
